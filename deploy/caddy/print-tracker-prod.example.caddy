print-tracker.example.com {
    encode gzip

    handle /api/* {
        reverse_proxy 127.0.0.1:3000
    }

    handle /health {
        reverse_proxy 127.0.0.1:3000
    }

    handle {
        root * /opt/print-tracker-prod/dist
        try_files {path} /index.html
        file_server
    }
}
